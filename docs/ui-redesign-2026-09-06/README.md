# AI Agent 编码工作台：界面审查与改造计划

日期：2026-09-06。状态：**UI-01–10（M0+M1）实施完成一轮**：UI-05 DONE，UI-01 部分 BLOCKED（交互走查不可达），其余 REVIEW 待人工验收；提交区间 `e4d2b82..15e50be`，详见 [任务清单](03-tasks.md) 第 8/9 节。

建议把现有产品收敛为一个「安静、紧凑、任务驱动」的桌面工作台：保留灰绿品牌识别和 Agent 优先的工作方式，以稳定导航、清晰正文、可切换的代码/成果区域组织功能。先修复空间分配与遮挡，再统一视觉，最后完善执行与验收体验。

## 阅读顺序

1. [现状审查与八步截图](01-audit.md)：当前功能、实际布局、问题证据与检查边界。
2. [改造设计方案](02-design.md)：信息架构、空间尺寸、视觉规范、各页面改造与实现边界。
3. [任务明细与跟踪清单](03-tasks.md)：32 项任务、依赖、验收标准、回归矩阵与推进记录。

## 最重要的判断

- **空间与层级优先于皮肤。** 多栏工作区的聊天列宽、编辑器、文件树、详情栏缺少统一分配策略；执行图还出现被文件栏遮挡的实际问题。
- **保留好的基础。** 现有主题令牌、Radix 基础组件、折叠工具轨迹、输入框、图编排和代码预览值得继续复用。
- **减少视觉负担。** 收敛侧栏大阴影、渐变、青绿色描边、大圆角及重复标题；让状态色、主要行动和代码差异成为视觉重点。
- **统一工作台，不统一所有数据。** 普通聊天、项目会话、架构画布共用视觉骨架，各自保留作用域与生命周期；架构设计图与运行 DAG 仍是两种对象。
- **把完成任务的路径做顺。** 发起 → 观察执行 → 查看产物/差异 → 验证 → 用户决定提交；日志、模型设置和工具配置按需展开。

## 证据范围

源码基线为 `04df59a`，检查时工作区无已有未提交改动。截图来自本次通过原生桌面 UI 操作打开的 `/Applications/JKCodingAgent.app`，版本 `0.2.1`，浅色模式；八张原始图像均为 1344×768 JPEG，已保存并重新打开检查。图像分辨率不等于已测得的 CSS viewport，文档中的布局尺寸以源码值或提案值为准。

项目 debug bundle 访问超时，因此使用已安装包。其版本号与仓库相同，**但没有构建 commit 证明，不能认定二进制与当前源码完全一致**。源码与截图分别引用；视觉问题先在当前开发构建复现后修复。未修改运行时代码、未发送模型任务、未执行图任务、未修改模型配置或 Git 内容。

截图仅保存在本地文档目录，包含当前项目名称、路径和历史任务片段；没有打开凭据明文字段。对外分享文档前应换成脱敏测试数据重新截图。

## 第一批建议

先执行任务清单中的 `UI-01 → UI-02/UI-03 → UI-04`：补齐当前构建基线，修复聊天溢出与图层遮挡，确定空间分配规则。不要把已确认的可用性问题拖到整体换肤之后。
