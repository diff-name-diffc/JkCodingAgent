# 审查报告问题追踪清单

> 来源：`docs/审查报告.txt`（约 240 条问题）。本清单按模块分组、统一编号。
> 状态：⬜ 待处理 ｜ ✅ 已修复 ｜ 🔧 已重构 ｜ ❌ 调研后否决（附理由）
> 处理方式由负责子智能体填写 `备注`。

## 分组总览

| 组 | 范围 | 问题数 | 负责 |
|----|------|--------|------|
| G1 | 工具策略层 tools/{spec,registry,result,provider,runtime,context}.rs | 23 | 子智能体 A |
| G2 | 命令执行门禁 builtin/{mod,shell,local_zsh,ssh,mcp,graph_report,submit_graph}.rs + ssh_review.rs | 22 | 子智能体 B |
| G3 | 文件/搜索/图片/浏览器工具 builtin/{common,filesystem,read_file,list_dir,search,image_edit,image_generation,browser}.rs | 24 | 子智能体 C |
| G4 | 图编排运行时 graph/{runner,node_exec,node_task,pi_rpc,harness}.rs | 20 | 子智能体 D |
| G5 | 图定义与存储 graph/{store,validate,types,commands,scheduler,verifier,receipt,input}.rs | 26 | 子智能体 E |
| G6 | DB 核心 db/{schema,sessions,messages,content,settings,mod,util}.rs | 21 | 子智能体 F |
| G7 | DB 其余 db/{keywords,categories,token_usage,artifacts,tool_runs,python_runs}.rs | 15 | 子智能体 G |
| G8 | 项目编排器 agents/project/* | 23 | 子智能体 H |
| G9 | 普通聊天+运行循环 agents/plain_chat + run_loop + common/config/prompt.rs | 17 | 子智能体 I |
| G10 | LLM/摘要/调试日志 llm.rs + summary.rs + debug.rs | 15 | 子智能体 J |
| G11 | 状态与命令 state/* + commands.rs | 14 | 子智能体 K |
| G12 | 子智能体管理 sub_agent/{config,manager,commands,db}.rs | 15 | 子智能体 L |
| G13 | 子智能体运行时 sub_agent/{tool,runtime}.rs | 9 | 子智能体 M |

---

## G1 工具策略层（tools/spec.rs, registry.rs, result.rs, provider.rs, runtime.rs, context.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G1-01 | security·critical | spec.rs:284-290 | access_for_name 兜底对未知工具返回全宽松权限并标记 Safe，exec/local_zsh 落入兜底 | 🔧 | 兜底 fail-closed；exec/local_zsh 显式 FULL_EFFECTS+ReviewRequired |
| G1-02 | maintainability·medium | spec.rs:224-226 | 工具名→策略映射分散在 7 个函数，应收敛为一张声明式配置表（要求完整重构） | 🔧 | 收敛为 TOOL_POLICY_TABLE 声明表 + 8 个断言测试 |
| G1-03 | bug·medium | spec.rs:263-266 | browser_read_text/browser_visual_analyze requires_network=false 与工具族矛盾；列入并行只读集不安全 | ✅ | 统一 requires_network=true；browser_* 移出并行只读集 |
| G1-04 | maintainability·low | spec.rs:250 | message 通知工具被归入 ToolCategory::Shell | ✅ | 改归 Other |
| G1-05 | bug·high | registry.rs:204 | execute_input 丢弃 effective_arguments，工具实际收到未补默认值的原始参数 | ✅ | 内置/动态执行均改用 effective_arguments |
| G1-06 | bug·high | registry.rs:149 | 动态 provider(MCP) specs 无条件 extend，绕过 allowed 白名单 | ✅ | 白名单同样过滤动态 specs + 测试 |
| G1-07 | bug·medium | registry.rs:147-149 | 内置与动态工具重名无唯一性校验，动态工具被静默遮蔽 | ✅ | 合并时去重（内置优先）+ 测试 |
| G1-08 | maintainability·low | registry.rs:110-111 | 遗留调试代码与冗余 early-return 分支 | ✅ | 已删除，统一走通用过滤路径 |
| G1-09 | bug·medium | result.rs:68-74 | from_text 仅凭「错误：」前缀判错误类型，未带前缀的真实错误被当作成功 | ✅ | TOOL_ERROR_PREFIX 契约；构造器强制补前缀 + 测试 |
| G1-10 | bug·medium | result.rs:121-124 | with_artifacts 覆盖而非追加 | ✅ | 改为 extend 追加语义 + 测试 |
| G1-11 | maintainability·low | result.rs:136-151 | run_metadata_json 永远返回 Some，Option 无意义 | ❌ | 改签名破坏 5 处范围外调用点；改"空返回 None"会丢审计统计，保持现状 |
| G1-12 | bug·medium | result.rs:142-144 | metadata 非对象值处理不一致（空对象跳过、空数组写入） | ✅ | 非对象包装为 {"value":…}，空对象/null 不写 + 测试 |
| G1-13 | maintainability·low | result.rs:16 | as_run_status 与 error_kind 状态字符串词表不一致 | ✅ | error_kind 与 as_run_status 对齐（仅影响新记录） |
| G1-14 | security·medium | provider.rs:12 | ToolProvider trait 未约定路径必须经工作区校验 | ✅ | trait 契约文档明确 |
| G1-15 | performance·medium | provider.rs:13-18 | trait 未约定阻塞操作必须 spawn_blocking | ✅ | trait 契约文档明确 |
| G1-16 | maintainability·medium | provider.rs:18-18 | execute 返回 None 语义与错误格式未定义 | ✅ | None=不处理；错误必须「错误：」前缀，契约成文 |
| G1-17 | bug·low | tools/runtime.rs:55 | timeout_secs.max(1) 改变 0 的语义 | ✅ | 0=不设统一超时，跳过超时包裹；语义成文 |
| G1-18 | bug·medium | tools/runtime.rs:108 | mark_tool_run_started_async 失败遗留"已创建未启动"脏记录，需补偿收尾 | ✅ | 失败补偿为 failed 终态并广播事件 |
| G1-19 | security·medium | tools/runtime.rs:81-85 | 未注册工具合成 spec 策略字段写入审计元数据，误导审计 | ✅ | 未注册仅记 registered:false，不落策略字段 |
| G1-20 | performance·medium | context.rs:39-40 | sub_agent_trace_events 无界增长 | ✅ | 由 G13 在 runtime.rs 落地：500 条上限 + traceTruncated 审计标记 + 容量测试 |
| G1-21 | security·medium | context.rs:29 | image_model_api_key 明文随 Clone 扩散 | ✅ | 手写 Debug 脱敏 <redacted>；SecretString 类型化列为跨界残余 |
| G1-22 | maintainability·medium | context.rs:36-38 | current_tool_call_id 等"一次调用内可变"状态随 Clone 独立复制 | ❌ | 现两条注入链均为 clone→写入→执行，无实际漂移；不变量已固化注释 |
| G1-23 | security·medium | context.rs:23-24 | workspace/extra_allowed_dirs 未在构造入口 canonicalize | ✅ | normalize_paths 在 ToolRuntime::execute_tool 统一调用；图节点直连 registry 的旁路已提示 graph 组 |

## G2 命令执行门禁（builtin/mod.rs, shell.rs, local_zsh.rs, ssh.rs, mcp.rs, graph_report.rs, submit_graph.rs, ssh_review.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G2-01 | security·high | builtin/mod.rs:24 | exec 工具仅靠静态黑名单，未接入 AI 审查门禁 | ✅ | exec 接入 ssh_review 审查链路（WorkspaceShell 目标类型） |
| G2-02 | security·high | builtin/mod.rs:51 | local_zsh/ssh_exec 在 ssh_review=None 时 fail-open 放行 | ✅ | 未配置审查时全部命令类工具 fail-closed 拦截并写审计 |
| G2-03 | bug·high | shell.rs:128-134 | 超时仅 kill 直接子进程，孙进程占管道导致 reader 永久阻塞 | ✅ | process_group(0)+超时整组 SIGKILL，shell/local_zsh 同修 |
| G2-04 | bug·medium | shell.rs:75 | spawn/捕获失败错误消息缺「错误：」前缀（两处） | ✅ | 已补前缀 |
| G2-05 | performance·low | shell.rs:24 | stdout+stderr 最大约 512KB 直接进 LLM 上下文 | ✅ | MAX_OUTPUT_BYTES 256KB→64KB（合计上限 128KB） |
| G2-06 | security·medium | local_zsh.rs:270-275 | allowed_root 校验恒真（死代码）；plain-chat-browser 工作区逃逸特判 | ✅ | 真实包含性校验，死代码移除，特判显式声明合法根 |
| G2-07 | bug·medium | local_zsh.rs:469-471 | audit.json 读-改-写无并发保护、非原子 | ✅ | run_dir 粒度锁映射 + 临时文件 rename 原子写 |
| G2-08 | maintainability·low | local_zsh.rs:175 | 多处错误消息缺「错误：」前缀 | ✅ | 全部补齐 |
| G2-09 | security·medium | ssh.rs:135-136 | 审查 payload 仅含 command，stdin 内容绕过审查 | ✅ | stdin 纳入送审（截断+分隔符包裹），参数描述同步警示 |
| G2-10 | other·low | ssh.rs:173-175 | 审查拦截提示缺「错误：」前缀 | ✅ | 已补前缀并附审计记录 |
| G2-11 | performance·low | ssh.rs:211-213 | 同 session 并发串行语义需文档化 | ✅ | 补并发语义文档注释 |
| G2-12 | security·high | mcp.rs:62-67 | MCP 工具桥无 AI 审查门禁、不受白名单约束 | ✅ | MCP 执行前强制同一审查门禁；白名单过滤已由 G1-06 在 registry 落实 |
| G2-13 | bug·medium | mcp.rs:66 | execute_tool 错误透传未保证「错误：」前缀 | ✅ | 桥接层 normalize_tool_error 归一化 |
| G2-14 | maintainability·low | mcp.rs:56-60 | 冗余预检查存在 TOCTOU，缓存缺失误报"工具未找到" | ✅ | 删除预检查，统一 ensure_recent |
| G2-15 | security·medium | mcp.rs:63-65 | args 未校验直接透传 MCP server（路径越权风险） | ✅ | 参数「..」静态拦截 + 完整参数进 AI 审查载荷 |
| G2-16 | bug·medium | builtin/graph_report.rs:43-46 | 协议壳无条件返回成功占位文本，应 fail-closed | ✅ | 未拦截环境返回「错误：」 |
| G2-17 | maintainability·low | builtin/graph_report.rs:15 | 文件名与工具名不一致，建议重命名 graph_plan_report.rs | 🔧 | git mv 重命名 + 引用同步 |
| G2-18 | maintainability·low | submit_graph.rs:0 | nodes 数组缺 minItems/maxItems | ✅ | minItems:1/maxItems:20 |
| G2-19 | documentation·low | submit_graph.rs:77 | description 未说明 MCP 工具以 source="aha" 提交 | ✅ | 描述已明确 |
| G2-20 | bug·high | ssh_review.rs:192 | allow 判定过宽/否定词覆盖不全，破坏 fail-closed | ✅ | 严格正向判定 + 10 项否定/存疑标记排除 + 5 组单测 |
| G2-21 | bug·medium | ssh_review.rs:127-129 | 超时错误被二次 map_err 包装，归类错误且文案重复 | ✅ | 先取内层 Result 再分别映射 |
| G2-22 | security·low | ssh_review.rs:175-177 | 审查 prompt 未对不可信字段做分隔/指令硬化 | ✅ | 审查须知声明 + 分隔符包裹防伪造 + 截断（命令 32K/stdin 4K） |

## G3 文件/搜索/图片/浏览器工具（builtin/common.rs, filesystem.rs, filesystem/, search.rs, image_edit.rs, image_generation.rs, browser.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G3-01 | security·medium | common.rs:180-181 | resolve_path 整体依赖 restrict_to_workspace 开关，false 时完全放行 | ✅ | false 时仍词法规范化+显式警告（fail-closed 保守化） |
| G3-02 | other·low | common.rs:210-212 | canonicalize_existing_prefix/resolve_path 错误缺「错误：」前缀 | ✅ | 已补前缀 |
| G3-03 | bug·medium | common.rs:237-239 | 词法归一化对前导 `..` 处理错误，`../foo` 被静默变成 `foo` | 🔧 | 保留无法抵消的前导 `..` + 单测 |
| G3-04 | performance·medium | common.rs:207-208 | resolve_path 等同步 I/O 在 async 上下文直接执行 | 🔧 | 路径解析/canonicalize/探测统一移入 spawn_blocking |
| G3-05 | bug·medium | filesystem.rs:71-74 | 创建父目录/写入/编辑失败错误缺「错误：」前缀 | ✅ | 已补前缀 |
| G3-06 | bug·high | filesystem.rs:130-132 | old_text 为空未拦截，content.replace("", …) 破坏文件 | ✅ | 显式拒绝空 old_text |
| G3-07 | bug·medium | filesystem.rs:141-143 | edit_file 读-改-写非原子，并发编辑丢改动 | 🔧 | 写前复检 size/mtime + 重读重试 3 次（未引 fs2 避免改 Cargo.toml） |
| G3-08 | bug·low | filesystem.rs:77-81 | 写入成功提示「字符」实为字节数 | ✅ | 改 chars().count() |
| G3-09 | bug·medium | read_file.rs:90-92 | 读取失败/超时错误缺「错误：」前缀 | ✅ | 已补前缀 |
| G3-10 | security·medium | read_file.rs:113-123 | 2MB 检查 TOCTOU，应基于打开句柄复检 | 🔧 | 先 open 再同句柄 metadata 复检 |
| G3-11 | performance·low | read_file.rs:71-73 | timeout 无法取消 spawn_blocking 读任务 | ✅ | file.take(2MB) 硬截断，读取有界 |
| G3-12 | performance·medium | list_dir.rs:229-232 | file_total_lines 完整读取大文件统计行数 | ✅ | >4MB 跳过并标注「行数未统计」 |
| G3-13 | maintainability·medium | list_dir.rs:159 | 目录级错误用 `[错误]` 前缀等格式不统一 | ✅ | 统一「错误：」前缀 |
| G3-14 | other·low | list_dir.rs:134-138 | 截断提示行使输出超过 max_entries | ✅ | 截断提示改独立尾注 |
| G3-15 | security·high | search.rs:340-343 | dir_path.join(pattern) 绝对/`..` pattern 逃逸工作区 | ✅ | 拼接后归一化+工作区校验，拒绝逃逸 + 单测 |
| G3-16 | bug·medium | search.rs:791-797 | grep 回退路径达 max_files 静默丢弃且计数低估 | ✅ | 补截断提示与部分统计说明 |
| G3-17 | maintainability·medium | search.rs:484-487 | grep/glob 多处错误缺「错误：」前缀 | ✅ | 已补前缀 |
| G3-18 | performance·low | search.rs:308 | GrepTool patterns×paths 并发无上限 | ✅ | 分批并发上限 4 |
| G3-19 | bug·medium | image_edit.rs:94 | to_str() fallback 回原始输入 + is_chat_image_path 绕过 resolve_path | ✅ | 非 UTF-8 显式报错；受信任目录注明；I/O 移阻塞线程 |
| G3-20 | other·medium | image_generation.rs:92 | 图片生成失败错误缺「错误：」前缀 | ✅ | 已补前缀 |
| G3-21 | bug·medium | image_generation.rs:48-51 | width/height 仅截断无范围校验，0/超大值不拒绝 | ✅ | 256–4096 范围校验，越界报错（edit_image 同步加固） |
| G3-22 | maintainability·low | image_generation.rs:47 | image_name 参数未使用与描述不符 | ✅ | 移除误导参数；字段删除列跨界残余（image_generator.rs） |
| G3-23 | security·high | browser.rs:144-147 | file:// URL 无工作区约束，可读任意本地文件 | ✅ | file: URL 解析+百分号解码+canonicalize，越工作区拒绝（不受 restrict 开关影响）+ 单测 |
| G3-24 | maintainability·medium | browser.rs:86-89 | 浏览器错误消息缺「错误：」前缀（多处） | ✅ | 统一补前缀（System 去掉 ❌） |
| G3-25 | security·medium | common.rs:276-277 | 本地命令静态黑名单/子串匹配可被参数重排/引号/变量拼接绕过 | 🔧 | 空白归一化+操作符分段+剥引号/命令替换/包装前缀；AI 审查门禁已由 G2 在 mod.rs 落地 |

## G4 图编排运行时（graph/runner.rs, node_exec.rs, node_task.rs, pi_rpc.rs, harness.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G4-01 | security·high | harness.rs:120-128 | harness 工具规格丢失安全元数据（readonly/review_required 全 false），可能绕过审查 | ✅ | pi_extension 默认 fail-closed；runtime_name 冲突校验；host 工具允许名单堵旁路 |
| G4-02 | security·high | harness.rs:31-33 | PiModelConfig 含明文 api_key 且派生 Serialize/Clone | ✅ | skip_serializing + sidecar_value() 唯一明文出口 |
| G4-03 | security·high | node_exec.rs:640-650 | redact 白名单全等匹配可被 X-Api-Key/client_secret 等变体绕过 | ✅ | contains/ends_with 扩展 + 变体单测 |
| G4-04 | bug·medium | node_exec.rs:180-184 | 取消/超时后在途 host 工具副作用继续执行 | ✅ | 3 秒有界收尾等待+显式上报；协作式取消令牌列跨界残余 |
| G4-05 | bug·medium | node_exec.rs:58 | 取消标志为真时任何 Err 被静默归类 Cancelled | ✅ | 真实故障一律 Failed，恰逢取消仅附注 |
| G4-06 | security·medium | node_exec.rs:621-629 | collect_affected_file 直接采用 sidecar 路径未校验 | ✅ | 工作区锚定归一化+包含校验，越界拒绝 + 单测 |
| G4-07 | security·medium | node_exec.rs:279-284 | 协议版本仅在 ready 校验，首条非 ready 消息可放行 | ✅ | enforce_handshake_order 门控 + 单测 |
| G4-08 | bug·medium | runner.rs:440-452 | 派发循环 `?`/panic 提前返回使在途节点停留 running | ✅ | 统一 task_error：停止派发→排空在途→整体返回 |
| G4-09 | bug·medium | runner.rs:369-376 | resume_rx.recv()=None 被当作恢复信号；wait_for_cancel 发送端丢弃后永久 pending | ✅ | None/Err 均按取消处理 + 单测 |
| G4-10 | security·medium | runner.rs:354-358 | 高危写检查点仅按 base_tool_group==Coding 判定，可写扩展工具绕过 | ✅ | node_may_write() 三因判定，目录缺失视为可写 |
| G4-11 | bug·high | node_task.rs:187 | 取消路径 list_node_runs 失败 unwrap_or_default 导致全节点被覆盖 cancelled | ✅ | 读取失败返回 Err 跳过取消（fail-closed） |
| G4-12 | other·medium | node_task.rs:172-175 | 取消复用 NodeSkipped 事件，缺 NodeCancelled 变体 | ✅ | 波次 3 完成：新增 NodeCancelled 变体（camelCase 协议）+ mark_node_cancelled 改发新变体 + 前端 graph-store reducer 分支 + 序列化锁定/reducer 测试；RunCancelled 不扩清单（hydrate 回源，理由成文） |
| G4-13 | maintainability·low | node_task.rs:188-192 | settled 判断把 running 视为已结算，脆弱 | ✅ | 显式 status == NODE_PENDING |
| G4-14 | bug·low | node_task.rs:194 | settled 未 trim node_id，resume 场景匹配不上 | ✅ | 两侧 id 均 trim |
| G4-15 | maintainability·low | node_task.rs:109 | 图状态 DB 写失败被忽略仍照常 emit | ✅ | 仅写库成功后广播，失败走整体失败收尾 |
| G4-16 | bug·high | pi_rpc.rs:305-310 | Drop Unix 分支无条件 return，组存活时不执行组杀，泄漏孤儿进程 | ✅ | 组存活显式组杀后再返回 |
| G4-17 | security·medium | pi_rpc.rs:323-326 | Windows 线程创建失败分支重开 PID 复用竞态 | ✅ | 句柄经 channel 移交；spawn 失败持句柄同步等待 taskkill |
| G4-18 | performance·medium | pi_rpc.rs:65 | catalog 响应读取无单行/总量限制，可 OOM | ✅ | read_bounded_line 单行 1MiB 上限，catalog 与节点流式读取统一 + 3 单测 |
| G4-19 | bug·medium | pi_rpc.rs:283-286 | terminate() 的 child.wait() 无超时 | ✅ | 5 秒超时，超时/失败交 Drop 兜底组杀 |
| G4-20 | security·medium | pi_rpc.rs:52 | discover_extension_tools 的 workspace 未规范化/校验 | ✅ | canonicalize + is_dir fail-closed；runner 侧同步规范化 |

## G5 图定义与存储（graph/store.rs, validate.rs, types.rs, commands.rs, scheduler.rs, verifier.rs, receipt.rs, input.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G5-01 | bug·high | validate.rs:286-294 | 缺失依赖被误判为环并跳过语义校验；trim 后重复 id 同样误报 | ✅ | 跳过指向不存在节点的边，重复 id 折叠，语义校验一次性给出 |
| G5-02 | bug·medium | validate.rs:147-151 | 重复 outputKey 时 producers last-wins，规则1判定误导 | ✅ | 歧义键跳过规则 1 祖先判定 |
| G5-03 | bug·low | validate.rs:158-161 | injectStateKeys 空串条目静默跳过，应报错 | ✅ | 空条目改报错（fail-fast） |
| G5-04 | bug·medium | validate.rs:207-211 | expectedFiles 冲突预检未做路径归一化，假阴性 | ✅ | 归一化（./、..、分隔符）后比较 |
| G5-05 | bug·medium | store.rs:0 | save_node_run 用 INSERT OR REPLACE 重置 rowid，节点顺序错乱 | ✅ | ON CONFLICT DO UPDATE，rowid 稳定 |
| G5-06 | bug·medium | store.rs:267-268 | fail_interrupted_runs 用 DEFERRED 事务与 Immediate 约定不一致 | ✅ | 改 TransactionBehavior::Immediate |
| G5-07 | maintainability·low | store.rs:136-139 | create_run 未显式校验 plan 存在 | ✅ | 事务内显式校验 + INSERT 补 context |
| G5-08 | bug·high | commands.rs:85-88 | 编辑图定义与启动运行 TOCTOU，无条件 UPDATE 可覆盖运行中计划 | ✅ | store 条件更新（AND status='draft'）+ 命令层重读校验双层封堵 |
| G5-09 | maintainability·medium | commands.rs:179-181 | panic 兜底恢复不足，计划可能长期停留 running | ✅ | 无论成败均复位 failed 并广播 |
| G5-10 | bug·medium | scheduler.rs:229-233 | claim/retry 缺内部前置校验，可重复执行/突破重试上限 | ✅ | claim 内置 pending+依赖+在途校验返回 bool；record_retry 内置预算校验 |
| G5-11 | maintainability·low | scheduler.rs:102 | cascade_initial_terminal 两步式契约无编译期保障 | ✅ | 级联并入 new()，两步契约消除 |
| G5-12 | bug·medium | verifier.rs:158-168 | 非空输出截断被归为"格式不符"，掩盖预算耗尽 | ✅ | 截断判定优先 + 原文预览 |
| G5-13 | performance·medium | verifier.rs:280-289 | failed 明细无总量预算，全失败时 prompt 膨胀 | ✅ | 8k 总量预算 + annotate_omitted |
| G5-14 | bug·medium | verifier.rs:186-187 | strict 首行"关键词+任意散文"放行，可伪造 PASS | ✅ | 解释性首行转全文扫描找独立结论行 |
| G5-15 | security·medium | receipt.rs:68-73 | sanitize_for_markdown 未转义 * # [ ] < > 等元字符，注入风险 | ✅ | 元字符全角转义，堵行内注入 |
| G5-16 | bug·medium | receipt.rs:196-200 | 丢弃所有非 succeeded 节点 usage，失败结算路径也不写 usage | ✅ | receipt 只跳 cached + runner Failed 分支 usage 落库管道已实现；残余：sidecar index.ts failed 消息上报 usage（TS 侧，范围外已记录） |
| G5-17 | performance·medium | receipt.rs:155-160 | 先全量 sanitize 再截断，大 state 值 CPU/内存峰值 | ✅ | 先按字符截断再清洗 |
| G5-18 | security·medium | input.rs:53 | 依赖 ID 未压平换行，可注入伪造顶级标题 | ✅ | dep 压平换行/回车后拼标题 |
| G5-19 | bug·low | input.rs:51 | 缺失依赖输出被静默跳过，下游模型无感知 | ✅ | 段尾标注「另有 N 个上游节点输出缺失」 |
| G5-20 | security·low | input.rs:73 | failure-reason 闭合标签仅精确替换，旧标签回显可混淆 | ✅ | 统一中和 </failure-reason 前缀 |
| G5-21 | bug·medium | input.rs:208 | 兜底重扫把未闭合开栏之后所有围栏永久忽略，伪摘要可注入 | ✅ | fence-reset 语义 + 反例测试，现有 4 测试兼容 |
| G5-22 | bug·medium | types.rs:159-163 | normalize_ids 未 trim 共享 state 定义侧 key | ✅ | state_keys.key 与 inherits planId/runId 同 trim |
| G5-23 | bug·low | types.rs:143-147 | upgrade_legacy 仅处理 version==2，其他旧版本静默失败 | ✅ | version < 当前版本统一升级，高版本交 validate 报错 |
| G5-24 | maintainability·low | types.rs:394-397 | 事件 duration_ms u64 与记录 Option<i64> 不一致 | ✅ | 事件统一 i64，与记录对齐 |
| G5-25 | maintainability·low | types.rs:358-359 | GraphRunEventPayload flatten JSON 形态需文档+测试锁定 | ✅ | 模块文档 + 序列化锁定测试 |
| G5-26 | maintainability·low | types.rs:172-175 | 状态/phase 裸 String 魔法值，应引入枚举/常量 | ✅ | 波次 3 完成：runner/node_task/node_exec/scheduler 字面量全部接入集中常量；enum 化经评估不做（落库值兼容/读取侧字符串世界/phase 开放集，理由写入 types.rs 注释）；测试字面量按约定保留（锁定落库值） |

## G6 DB 核心（db/schema.rs, sessions.rs, messages.rs, content.rs, settings.rs, mod.rs, util.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G6-01 | bug·high | schema.rs:281-286 | ensure kind 列 DEFAULT 'project' 导致旧库会话在 v6 被整体删除 | ✅ | 缺列默认值改 'chat' + legacy 库迁移测试 |
| G6-02 | bug·high | schema.rs:1541 | VACUUM INTO ? 绑定参数语法错误，快照恒失败 | ✅ | 改字符串字面量（单引号双写转义），原失败测试转绿 |
| G6-03 | bug·medium | schema.rs:420-421 | v6 迁移遗漏 dispatcher_tool_runs 清理 | ✅ | SCHEMA_VERSION 26→27，新增 v27 事务迁移清理悬空记录 |
| G6-04 | bug·medium | schema.rs:32 | 快路径 `>=` 放行更高版本库，应 `==` 且 `>` 报错 | ✅ | > 直接 bail 提示升级，== 走快路径 + 测试 |
| G6-05 | bug·low | schema.rs:927-930 | token_usage 迁移 COALESCE 归一可能 UNIQUE 冲突回滚 | ✅ | GROUP BY 聚合去重（SUM/MAX）+ 缺列常量兜底 + 测试 |
| G6-06 | bug·medium | sessions.rs:0 | 创建会话两条 INSERT 无事务保护 | ✅ | 两条 INSERT 包入同一事务 |
| G6-07 | bug·medium | sessions.rs:398-405 | 删除会话未校验 dispatcher_sessions.kind，可误删另一类型 | ✅ | 事务内先校验 kind，不符即报错 |
| G6-08 | bug·medium | sessions.rs:406-408 | 文件清理失败使已提交的删除误报失败，应 best-effort | ✅ | 改 best-effort 留痕；clear_messages 同类问题一并修复 |
| G6-09 | bug·medium | messages.rs:560-565 | truncate_messages_from 未清理 chat_images 记录与磁盘文件 | ✅ | 事务内删记录、提交后删文件，含幸存引用检查防误删 |
| G6-10 | bug·high | messages.rs:368 | load_llm_history 缺 visible=1 过滤，隐藏消息可注入 LLM 上下文 | ✅ | 加 visible=1；对话窗口锚点同步一致 |
| G6-11 | bug·medium | messages.rs:515-519 | 删除/截断/分类更新未同步所有会话表 updated_at | ✅ | 三张会话表 updated_at 事务内同步 |
| G6-12 | security·medium | content.rs:105-108 | safe_absolute_image_path 未解析符号链接，可删除目录外文件 | ✅ | canonicalize 后再前缀校验，不存在时父目录解析拼接 |
| G6-13 | bug·medium | content.rs:70-74 | &segments_json[..500] 在多字节 UTF-8 边界 panic | ✅ | floor_char_boundary(500) |
| G6-14 | bug·medium | content.rs:147 | ON CONFLICT(image_id) 无条件覆盖归属字段 | ✅ | 归属字段不覆盖 + WHERE 限同一 workspace/message |
| G6-15 | bug·medium | content.rs:213-216 | 任一坏路径阻断整个图片清理流程 | ✅ | 坏路径跳过留痕，不阻断 DB 删除与其余清理 |
| G6-16 | bug·low | content.rs:68-69 | JSON 解析失败静默降级为空列表无日志 | ❌ | 核实初始提交即有 eprintln 日志，报告针对旧形态；顺带为 util.rs 真正静默处补日志 |
| G6-17 | bug·high | settings.rs:317 | save_settings_v2 与读取端 normalize 不对称，往返丢数据 | ✅ | 保存前统一 normalize 并直接返回规范化结果，写读对称 |
| G6-18 | bug·medium | db/mod.rs:98-107 | total_chars 累加字节数而非字符数，CJK 偏差 3 倍 | ✅ | 按 chars().count() 统计；序列化失败保守估算+告警 |
| G6-19 | maintainability·low | db/mod.rs:63-65 | pool() pub 暴露原始连接池，应收窄 pub(crate) | ✅ | 收窄 pub(crate)，内部调用点编译通过 |
| G6-20 | bug·low | util.rs:43-46 | thinking_elapsed_ms 负值导致整行映射失败 | ✅ | 负值 clamp 到 0，坏行不再拖垮整表加载 |
| G6-21 | bug·low | util.rs:82-84 | DB 行映射/错误信息依赖硬编码列位置 | ✅ | 按列名读取 + 错误携带真实列索引 |

## G7 DB 其余（db/keywords.rs, categories.rs, token_usage.rs, artifacts.rs, tool_runs.rs, python_runs.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G7-01 | bug·high | keywords.rs:85-90 | 事务内错误被 let _ 丢弃；INSERT OR REPLACE 重写 created_at 不聚合权重 | ✅ | 错误 `?` 传播；UPSERT 聚合权重保留 created_at + 2 测试 |
| G7-02 | performance·medium | keywords.rs:231-236 | matched_keywords 循环内 N+1 查询 | ✅ | 批量取回后内存过滤，消除 N+1 |
| G7-03 | maintainability·medium | keywords.rs:15-20 | workspace_id 实际存会话 id，命名误导，应改名 session_id（含迁移） | ✅ | 波次 3 完成：SCHEMA_VERSION 28 迁移（RENAME COLUMN + 索引重建 + FK 校验）；keywords/sessions/messages/commands + 前端 types/SessionPanel/use-chat-queries + backfill 脚本（清单外发现）全部同步；313 测试 + pnpm build 通过 |
| G7-04 | bug·medium | categories.rs:82-88 | query_row 错误被 unwrap_or(-1) 吞掉 | ✅ | context + `?` 向上传播 |
| G7-05 | bug·low | categories.rs:217-221 | delete_chat_category 对不存在分类返回 Ok，应 Option | ✅ | EXISTS 校验，不存在即 bail 区分（Option 签名需动命令层，未改） |
| G7-06 | bug·medium | categories.rs:325-332 | 单行损坏 JSON 使整条分类配置功能永久失败，无自愈 | ✅ | 读降级 + repair_corrupt 修复函数 + 测试 |
| G7-07 | performance·medium | categories.rs:226-231 | 配置读取路径每次 Immediate 写事务+backfill 写回 | ✅ | 热路径无锁纯读，仅缺行时按需 backfill |
| G7-08 | bug·medium | token_usage.rs:62-64 | total_tokens 与 prompt+completion 独立累加口径矛盾 | ✅ | upsert 统一 prompt+completion 重算 total，存量自愈 |
| G7-09 | other·low | token_usage.rs:65 | cached_tokens 逐次累加语义不可解释 | ❌ | 否决：累加为会话生命周期计费口径，改覆盖反丢信息；已补注释 |
| G7-10 | bug·medium | token_usage.rs:174-179 | from_sql_value 未知值静默归类 Primary | ✅ | 返回 Result，未知值报错；list 兜底留痕 + 2 测试 |
| G7-11 | performance·medium | artifacts.rs:56-58 | 查询/事件一次性加载大量记录无分页 | ✅ | 波次 3 完成：Finished 负载改 {workspaceId, messageCount}（新增计数查询，不再全量加载）；前端 finished 改调 dispatcher_list_messages 拉全量；AgentTurn.messages 移除；孤立的 list_visible_messages_async 删除 |
| G7-12 | bug·medium | artifacts.rs:71-72 | DB i64 `as` 强转 usize/u32 可静默回绕 | ✅ | 统一 try_from + 列索引转换错误，同类强转一并加固 |
| G7-13 | bug·medium | tool_runs.rs:110-114 | finish_tool_run 可选字段语义不一致、无状态机约束 | ✅ | COALESCE 保留语义 + 终态守卫（幂等）+ 4 测试 |
| G7-14 | maintainability·low | tool_runs.rs:116-119 | duration_ms 依赖 julianday 解析文本时间戳 | ✅ | Rust 侧 chrono 计算 + Option<i64> 容错 |
| G7-15 | bug·low | python_runs.rs:0 | RFC3339 文本时间戳精度可变，字符串排序可错位 | ✅ | julianday 数值排序 + 确定性 tie-break + 2 测试 |

## G8 项目编排器（agents/project/*）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G8-01 | bug·medium | helpers.rs:70-81 | token 用量持久化 fire-and-forget，退出/故障静默丢失 | ✅ | 返回 JoinHandle + 失败重试一次 + 持久化警告；收口统一 await |
| G8-02 | maintainability·low | helpers.rs:115-117 | 事件推送 let _ 静默吞错无日志 | ✅ | emit 失败记持久化警告日志 |
| G8-03 | other·low | helpers.rs:93-94 | 会话用量落库与运行统计口径分裂 | ✅ | 句柄随批次 await，统计与 DB 同源，失败留警告 |
| G8-04 | other·low | helpers.rs:52-54 | exec 硬编码不可重试，恢复能力下降 | ❌ | 否决：自动重试会绕过审查门禁重审；错误回喂后新 exec 重新过门禁，补注释固化设计 |
| G8-05 | other·low | iteration.rs:159 | 错误文案缺「错误：」前缀 | ✅ | empty_llm_response_error 补前缀 |
| G8-06 | maintainability·medium | iteration.rs:30-38 | get_session_title/latest_user_message 失败静默降级无日志 | ✅ | 记持久化警告（含 workspace_id）后降级 |
| G8-07 | security·low | iteration.rs:48-50 | extra_allowed_dirs 放行 ~/.jkcodingagent 需注释说明 | ✅ | 补必要性注释（勿扩大范围） |
| G8-08 | bug·low | iteration.rs:199-202 | usage_tracker=None 分支静默丢用量 | ✅ | tracker 改必传，删除 None 分支 |
| G8-09 | maintainability·medium | mod.rs:154 | 持 Mutex 时嵌套获取 provider 锁，锁序隐患 | 🔧 | 两把 Mutex 合并为单一 Models 状态锁，消除嵌套 |
| G8-10 | bug·medium | mod.rs:142-147 | apply_settings_v2 空值处理不一致（残留/覆盖） | ✅ | 摘要凭据显式覆盖（清空回退聊天凭据）；视觉无有效条目保留现有 |
| G8-11 | maintainability·low | mod.rs:121-124 | 设置更新两段两锁非原子，可观察混合状态 | 🔧 | 锁外解析凭据，单锁内固定顺序原子应用 |
| G8-12 | security·medium | turn.rs:31-35 | project_path 未 canonicalize/工作区包含校验 | ✅ | 命令边界校验：绝对路径+禁 ..+符号链接解析+受管项目列表命中，全程 spawn_blocking |
| G8-13 | maintainability·low | turn.rs:72-74 | eprintln 不落盘，应走统一日志 | ✅ | 持久化 orchestrator.log（5MB 滚动），stderr 兜底 |
| G8-14 | bug·high | graph_submit.rs:185-189 | 可重试工具错误静默丢弃同轮已成功的 submit_graph 协议动作 | ✅ | 协议动作优先收口，先于可重试错误判断 |
| G8-15 | bug·low | graph_submit.rs:289-291 | 继承 run 状态 denylist 应改 allowlist | ✅ | 终态白名单：仅 completed/failed 可继承 |
| G8-16 | bug·medium | graph_report.rs:152-153 | 空串 state_json 解析失败告警误导模型 | ✅ | 空串等价无 state 键静默跳过，仅非空解析失败告警 |
| G8-17 | bug·low | graph_report.rs:45-47 | 显式 planId 不存在时返回不准确提示 | ✅ | 「错误：」前缀提示，走可重试通道交回模型纠正 |
| G8-18 | maintainability·low | graph_report.rs:53 | runs.first() 与 node_runs 一致性依赖隐式契约 | ✅ | 优先 latest_run_id 对应 run，节点明细按同 run_id 过滤 + 不一致告警 |
| G8-19 | security·medium | prompt.rs:260-264 | skills/USER.md/MEMORY.md 符号链接未校验可注入工作区外内容 | ✅ | 全部 canonicalize 校验在 root 内，越界跳过留痕 |
| G8-20 | bug·medium | prompt.rs:249-255 | 提示词文件无大小上限；任一读取失败使编排器无法启动 | ✅ | 64KB 上限超限跳过告警；读取失败跳过留痕不阻断启动 |
| G8-21 | performance·low | prompt.rs:128 | 每次迭代全量 clone static_content | ✅ | 按 &str 引用直接装配，消除逐迭代中间克隆 |
| G8-22 | maintainability·low | prompt.rs:122-125 | _workspace_id 参数未使用误导维护者 | ✅ | 删除参数及调用点传参 |
| G8-23 | maintainability·low | tool_exec.rs:373-381 | 串行/并行分支 workspace 来源不一致 | ✅ | 统一取 tool_context.workspace |

## G9 普通聊天+运行循环（agents/plain_chat/*, run_loop/*, agent/common.rs, config.rs, prompt.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G9-01 | bug·high | plain_chat/mod.rs:294-296 | 取消/错误提前返回时 tool run 不收尾，永久悬挂 | ✅ | execute_all_tools 重构为执行→持久化→收尾一体化，三条异常路径均补偿收敛终态 |
| G9-02 | performance·high | plain_chat/mod.rs:555-557 | async 路径直接执行同步 SQLite/文件阻塞 I/O | ✅ | get_settings_v2/get_enabled_for_session 移入 spawn_blocking + run 入口预热缓存；跨界：state 侧同名调用已由 G11 闭环 |
| G9-03 | security·medium | plain_chat/mod.rs:903-905 | allowed_tools 为空 fail-open 放行全部工具；category_context 不清理 | ✅ | 「空=全部放行」显式文档化（附理由）；设置重应用同步清除 category_context |
| G9-04 | bug·medium | plain_chat/mod.rs:523 | 所有聊天会话共享固定工作区，缺会话级隔离 | ✅ | 会话子目录（安全字符清洗+FNV-1a 哈希后缀）+ 2 测试 |
| G9-05 | bug·medium | run_loop/agent_loop.rs:43-45 | append 未套用 should_keep_llm_message 过滤，跨 run 上下文不一致 | ✅ | 过滤抽为 common 公共函数，append 同口径套用 + 4 测试；残余：db 侧同口径函数待 delegate |
| G9-06 | maintainability·low | run_loop/agent_loop.rs:41-45 | append 持久化依赖调用方，无契约/兜底 | ✅ | 类型级+方法级强契约文档 |
| G9-07 | bug·medium | run_loop/types.rs:53-57 | tool_call_id 为 Option，事件无法配对；ToolPlanned/Started 字段重复 | ✅ | 波次 3 完成：tool_call_id 必填 String + ToolFinished 补 arguments 对称；前端 payload 类型与匹配逻辑同步收敛 |
| G9-08 | bug·medium | run_loop/types.rs:37-40 | AssistantDelta 无序号/结束标记，乱序无法检测 | ✅ | 波次 3 完成：seq 单调递增（AtomicU64，正文/思考共享计数）+ AssistantMessage.last_seq 贯穿；前端类型承载 + 注释 |
| G9-09 | maintainability·low | run_loop/types.rs:63-68 | ToolSummary* 预留事件 allow(dead_code) 掩盖 | ✅ | 移除压制属性，补启用条件与 result_mode 一致性约束文档；评估确认前端已有 handler，属协议预留非死代码 |
| G9-10 | maintainability·low | run_loop/types.rs:8-10 | AgentTurn 缺 Clone | ✅ | 补 Clone |
| G9-11 | bug·medium | common.rs:106-107 | with_usage_paused 非 panic/取消安全，应 RAII guard | 🔧 | UsagePauseGuard：构造 pause、Drop 无条件 resume |
| G9-12 | maintainability·low | common.rs:168-171 | stream_llm_response 终止事件契约依赖调用方自觉 | ✅ | 采用报告第二选项：契约显式成文（补发会双重事件） |
| G9-13 | bug·low | common.rs:607 | ModelSwitched 仅比较模型名，同网关切换无感知 | ✅ | model+api_base+api_key 三项同 provider 比较 |
| G9-14 | bug·medium | common.rs:556 | 工具参数序列化失败静默替换为 {} | ✅ | 统一 serialize_tool_arguments 记「错误：」日志后降级；Result 上抛列跨界残余（tool_exec 调用点） |
| G9-15 | bug·medium | config.rs:156-158 | 模型配置缺 API key 未尽早失败 | ✅ | validate_provider_completeness 命令入口显式失败；load() 保留 env 回退并文档说明 |
| G9-16 | maintainability·medium | config.rs:178-183 | 提示词文件 fs::write 非原子，损坏后无法自愈 | ✅ | 临时文件+rename 原子写（pid 后缀防并发）+ 测试 |
| G9-17 | bug·low | prompt.rs:3-5 | PlainChatAgent 不每轮重建系统提示，文档与实际不符 | ✅ | 每轮重建 system+纯历史（AgentLoop::history()），文档修正 |

## G10 LLM/摘要/调试日志（llm.rs, summary.rs, debug.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G10-01 | security·high | llm.rs:814-817 | local_image_path 未限制到聊天图片目录，可外传任意文件 | ✅ | 复用 is_chat_image_path 包含性校验，越界/.. 拒绝 |
| G10-02 | bug·medium | llm.rs:757-764 | content 文本在仅含图片 part 时被静默丢弃 | ✅ | 无 Text part 且 content 非空时补为首个 Text part |
| G10-03 | bug·high | llm.rs:966-967 | enable_thinking 无条件序列化，严格 provider 400 全挂 | ✅ | Option+skip_serializing_if；400 重试同时置 None 并扩关键词 |
| G10-04 | bug·medium | llm.rs:869-870 | fetch_models 无总超时，可无限等待 | ✅ | 30s 总超时 |
| G10-05 | bug·low | llm.rs:524-528 | SSE 行解析要求 "data: " 含空格，data:{...} 被丢弃 | ✅ | parse_sse_data_line：strip_prefix+trim_start，兼容两种格式 |
| G10-06 | security·medium | summary.rs:618-619 | 不可信对话内容包装进 system 消息，prompt injection | ✅ | system 只留任务规则，数据移入 user 消息并标注不可信 |
| G10-07 | bug·medium | summary.rs:276-280 | 区块截断启发式可被正文中的标签提前切断 | ✅ | 优先闭合标签截止，缺失才回退 + 告警 |
| G10-08 | bug·medium | summary.rs:322-328 | 退出状态启发式误匹配普通日志行 | ✅ | 仅匹配 exit code / 退出状态 明确模式，无匹配不编造 |
| G10-09 | bug·low | summary.rs:362 | 行号前缀剥离误拆含 \| 的代码行 | ✅ | 仅剥离「行首数字+可选空格+\|」形式 |
| G10-10 | maintainability·low | summary.rs:236-239 | 空格折叠死代码分支 | 🔧 | 删死代码，明确折叠策略（保留行首缩进） |
| G10-11 | maintainability·medium | summary.rs:503 | 关键字动作解析失败静默吞掉 | ✅ | 告警+返回空数组（Result 方案需改 commands.rs，采用报告第二选项） |
| G10-12 | performance·high | debug.rs:80-83 | 持锁期间做文件 I/O，应重构为后台写入线程+通道 | 🔧 | 专用后台写入线程 + mpsc，log() 仅无锁 send，彻底消除持锁 I/O |
| G10-13 | bug·medium | debug.rs:59-65 | log() 无 runtime 时 spawn_blocking panic | ✅ | 随 G10-12 解决：std 线程链路不依赖 tokio runtime |
| G10-14 | bug·low | debug.rs:71 | 序列化失败信息未转义拼进 JSON | ✅ | serde_json 转义后嵌入 |
| G10-15 | performance·medium | debug.rs:85-90 | agent.debug 无大小/轮转策略 | ✅ | >10MB 滚动为 agent.debug.old（仅留一份） |

## G11 状态与命令（state/mod.rs, run.rs, tool_catalog.rs, generation, agent/commands.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G11-01 | bug·high | state/mod.rs:116-120 | get_settings_v2().expect() DB 失败直接 panic | 🔧 | build_run_agent 改 async + spawn_blocking，Result 透传可读错误 |
| G11-02 | performance·medium | state/mod.rs:57-60 | new() 主线程同步 DB 打开/迁移/加载 | 🔧 | DispatcherState::new 改 async，阻塞 I/O 全入 spawn_blocking；setup 失败弹对话框退出 |
| G11-03 | security·high | state/mod.rs:174-178 | list_agent_tools/ssh_workspace_for_context 的 project_path 未校验 | ✅ | canonicalize + 包含校验（应用数据目录/已注册项目）；命令入口同步加固。注：root_dir 为应用数据目录，报告原建议会误拒合法项目，已按实际信任模型调整 |
| G11-04 | maintainability·medium | state/mod.rs:62-64 | SubAgentManager::load_all 失败仅 eprintln 后继续 | ✅ | 失败置 sub_agent_manager=None 禁用能力并告警 |
| G11-05 | bug·medium | tool_catalog.rs:36-40 | workspace=None 分支硬编码 "." 且忽略 include_dynamic | ✅ | 语义显式化：仅静态工具 + 契约注释 + 请求动态工具时警告 |
| G11-06 | performance·medium | tool_catalog.rs:25-26 | 全同步枚举工具 spec 在 async 命令中调用 | ✅ | 注册表构建整体 spawn_blocking |
| G11-07 | bug·medium | tool_catalog.rs:9-11 | ToolCatalog 缓存从不刷新，子智能体增删后过期 | 🔧 | refresh 原子替换 + 初始目录补齐子智能体工具 + 读取即重建 |
| G11-08 | maintainability·low | tool_catalog.rs:50-51 | 同名工具去重静默掩盖冲突 | ✅ | 去重前检测并打印警告，保留策略成文 |
| G11-09 | bug·high | state/run.rs:43-45 | ActiveRunHandle/GraphRunHandle 无生命周期清理，panic 后永久残留 | 🔧 | ActiveRunHandle RAII Drop 清理；GraphRunHandle 以接收端存活检测+begin 兜底回收等价实现 |
| G11-10 | bug·medium | state/run.rs:48-50 | stop/finish 缺代际标识，迟到请求误伤新 run | 🔧 | 同锁分配递增 epoch，清理/finish 仅作用同代条目 |
| G11-11 | bug·medium | state/run.rs:123-128 | cancel/resume 双通道无统一状态，可绕过取消 | ✅ | resume 前强制复查 cancel；单一 GraphControl 通道彻底形态列跨界残余 |
| G11-12 | performance·low | state/run.rs:101 | resume 无界 channel，陈旧信号跳过确认暂停 | ✅ | 容量 1 有界 mpsc + try_send 去重（runner.rs 一行类型适配） |
| G11-13 | bug·medium | commands.rs:47-49 | generation 守卫 begin/finish 竞态、异常路径泄漏 | 🔧 | 分配/登记同锁原子化 + GenerationGuard Drop 自动结算 |
| G11-14 | bug·medium | commands.rs:190-191 | 关键字提取 user/assistant 配对未校验同一轮 | ✅ | latest_qa_pair 纯函数：末条角色决定配对 + 4 测试 |

## G12 子智能体管理（sub_agent/config.rs, manager.rs, commands.rs, db.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G12-01 | bug·medium | config.rs:104-106 | user_prompt_template 空值未校验 | ✅ | trim 后非空校验 |
| G12-02 | bug·medium | config.rs:32-33 | inherit_from_parent=false 时 model_name/api_base 缺失未校验 | ✅ | model_name 必填；api_base/key 保留父级回退并注释 |
| G12-03 | bug·medium | config.rs:40-41 | timeout_secs 无范围校验 | ✅ | 1–3600 范围校验 |
| G12-04 | bug·medium | config.rs:44-47 | created_at/updated_at 默认 0（1970 时间戳） | ✅ | default_timestamp 当前毫秒默认函数 |
| G12-05 | bug·medium | config.rs:107-109 | allowed_tools 未校验工具名真实注册 | ✅ | 静态注册名集合校验（create/update 入口） |
| G12-06 | bug·medium | config.rs:116-118 | temperature 范围校验拦不住 NaN | ✅ | is_finite + contains 拦截 NaN/无穷 |
| G12-07 | bug·high | manager.rs:61-63 | update 缓存键与 DB 键不一致，缓存分叉 | ✅ | agent_id == id 强制校验，不一致即报错 |
| G12-08 | bug·high | manager.rs:36-41 | 单条解析失败后仍用部分成功结果覆盖 cache | ✅ | 解析失败直接 Err，旧缓存保持不动 |
| G12-09 | maintainability·medium | manager.rs:92-94 | set_global_enabled 只写 DB 不刷新缓存 | ✅ | 写后 load_all 刷新 |
| G12-10 | performance·high | manager.rs:25-26 | manager/命令链路 async 中直接同步 rusqlite | ✅ | commands 全部 spawn_blocking 包裹；跨界：plain_chat/tool.rs 调用点已分别移交 G9/G13 |
| G12-11 | bug·medium | commands.rs:48-50 | config.agent_id 与路径 id 不一致被静默纠正 | ✅ | 不一致报错，一致/为空才回填 |
| G12-12 | bug·medium | commands.rs:109-111 | set_global_enabled 未校验 id 存在性 | ✅ | list_all 校验存在性，未知 id 明确报错 |
| G12-13 | maintainability·low | commands.rs:77-79 | seed_browser_force 无确认机制覆盖用户定制 | ❌ | 需命令签名变更+前端确认 UI 配套（禁改签名），列跨界待后续 |
| G12-14 | bug·medium | db.rs:400-401 | json_each 展开前未 json_valid，任一分类坏 JSON 全挂 | ✅ | JOIN ON 加 json_valid 过滤 + 回归测试 |
| G12-15 | maintainability·low | db.rs:117-121 | 硬编码 'browser-agent' + unwrap_or(0) 吞错 | ✅ | config.agent_id 参数化，错误传播 |

## G13 子智能体运行时（sub_agent/tool.rs, runtime.rs）

| 编号 | 严重度 | 位置 | 问题摘要 | 状态 | 备注 |
|------|--------|------|----------|------|------|
| G13-01 | bug·medium | tool.rs:203-208 | call_sub_agent 失败消息缺「错误：」前缀（多处） | ✅ | 5 处失败包装统一补前缀 |
| G13-02 | bug·low | tool.rs:158-165 | agent_id/task 未 trim 即校验 | ✅ | trim 后判空 |
| G13-03 | bug·low | tool.rs:22-24 | SUB_AGENT_FAILURE_PREFIX 字符串协议可碰撞 | ✅ | 透传前检测碰撞并转义包装（原文保留）+ 3 测试；结构化协议列跨界残余 |
| G13-04 | bug·high | runtime.rs:595-602 | 工具超时仅中止等待，底层阻塞/子进程继续执行 | ✅ | 整体剩余预算 timeout + fail-closed 显式上报；底层真正终止需工具层支持（跨界记录） |
| G13-05 | bug·high | runtime.rs:479 | tool_error_seen 一次性标志缺陷，重试/收口要求矛盾 | 🔧 | 按工具名记录失败轮数的状态机 + 批内统一决策 + 纯函数测试 |
| G13-06 | bug·high | runtime.rs:403-404 | force_final_response 后模型仍返回工具调用会绕过收口 | ✅ | 绝不执行工具：有 content 强制收口，无则错误退出 |
| G13-07 | performance·medium | runtime.rs:321 | 消息历史无界增长，无上下文裁剪 | 🔧 | 按轮次滑动窗口（system+首轮 user+最近 8 轮 ≤120k 字符）+ 占位说明 + Option 免 clone + 4 测试 |
| G13-08 | performance·medium | runtime.rs:241 | trace_events_json 持锁序列化 | ✅ | 锁内 clone、锁外序列化 |
| G13-09 | style·low | runtime.rs:486 | 重试/收口提示缺「错误：」前缀 | ✅ | 已补前缀（随 G13-05 重写落地） |

---

## 处理记录

### 波次 1 · G1 工具策略层（policy-fixer，2026-08-11 完成）
23 项：18 ✅ + 2 🔧 + 3 ❌。重点：spec.rs 七个映射函数完整重构为声明式策略表（兜底 fail-closed）；registry 白名单覆盖动态工具、effective_arguments 真正生效；result.rs 错误前缀契约强制化。
跨界移交：trace 缓冲容量治理 → G13（sub_agent/runtime.rs）；图节点旁路未过 normalize_paths → 已提示 G4。
风险：error_kind 词表变更仅影响新记录；browser 只读工具改串行（保守化）。

### 波次 1 · G2 命令执行门禁（gate-fixer，完成）
22 项：21 ✅ + 1 🔧。重点：exec/local_zsh/ssh/MCP 全部接入 AI 审查门禁且未配置时 fail-closed；shell 超时整组进程杀；allow 判定严格正向 + 否定标记排除；stdin 纳入送审；prompt 指令硬化。全量测试 235/235 通过。
行为变更（预期）：未配置审查模型时命令类工具全部拒绝执行。
残余：local_zsh.rs 686 行（原 638）超 500 行规范，渲染辅助函数列为后续拆分候选。

### 波次 1 · G4 图编排运行时（graphrt-fixer，完成）
19 ✅ + G4-12 ⏸（调查结论：新增 NodeCancelled 变体方案，留波次 3）。重点：harness 安全元数据 fail-closed + 宿主工具白名单堵旁路；redact 变体覆盖；Drop 组杀逻辑漏洞修复；resume/cancel 通道语义反转修复（None/Err 一律按取消）；node_may_write 高危写判定；bounded line reader 防 OOM。graph 子套件 116/116 通过。
G5-16 闭环结论：runner Failed 分支 usage 落库管道已实现（NodeExecOutcome::Failed 携带 usage_json）；残余为 sidecar TS 侧 failed 消息上报 usage（范围外，已记录）。
残余：工具级协作式取消（G4-04 完整方案）需改工具层。

### 波次 1 · G5 图定义与存储（graphdata-fixer，完成）
26 项：25 ✅ + G5-26 🔧（常量集中+文档完成，完整枚举化留波次 3）。重点：缺失依赖不再误判为环；图定义编辑 TOCTOU 双层封堵（条件 UPDATE + 重读校验）；save_node_run rowid 稳定；验证/回执/输入构造多处注入防护与预算控制。graph 子套件 116/116。

### 波次 1 · G6 DB 核心（dbcore-fixer，完成）
21 项：20 ✅ + 1 ❌（G6-16 报告不准确，日志本就存在）。重点：SCHEMA_VERSION 26→27（v27 清理悬空 tool_runs）；kind 缺列默认 'chat' 防数据丢失；VACUUM INTO 语法修复（快照真正生成）；高版本库拒绝打开；visible=1 过滤；会话删除 kind 校验；图片路径 canonicalize 防符号链接穿越；settings 保存规范化对称化。db 测试 37/37 通过（含 5 个新迁移测试）。
风险：token 估算改按字符（中文估算值降为约 1/3）；旧版应用打开新库会明确报错。

### 波次 1 · G7 DB 其余（dbmisc-fixer，完成）
15 项：12 ✅ + 2 ❌ + G7-03 ⏸（调查完毕：调用点清单 + RENAME COLUMN 迁移方案，留波次 3）。
否决理由：G7-09 cached_tokens 累加即计费口径（补注释）；G7-11 artifacts 无列表查询，真正重负载为 Finished 事件全量消息（移交 G9）。
重点：keywords UPSERT 聚合权重；分类配置无锁读 + 坏行自愈；token total 口径统一；tool_runs 状态机单向守卫。235/235 通过。
移交：Finished 事件轻量化 → G9；build_plain_chat_agent 同步 DB 阻塞 → G11（已由 G11 闭环：改 async + 合并为一次 spawn_blocking）。

### 波次 1 · G3 文件/搜索/图片/浏览器工具（fstools-fixer，完成）
25 项：18 ✅ + 7 🔧。重点：file:// URL 工作区拘禁（canonicalize+拒绝越界）；glob pattern 逃逸封堵；edit_file 空 old_text 拦截 + 写前 mtime 复检重试；read_file TOCTOU 修复 + take(2MB) 有界读；is_dangerous 黑名单强化（归一化/分段/剥引号/剥包装前缀）；同步路径 I/O 移入 spawn_blocking。新增 11 条单测。
残余：ImageGenerationInput.image_name 字段删除需改 tools/image_generator.rs（范围外）；edit_file 未引 fs2 硬锁（避免改 Cargo.toml）。

### 波次 1 集成验证（2026-08-11）
`cargo check`：0 错误（3 条 dead-code 警告待清理）；`cargo test --lib`：**239 passed / 0 failed**。

### 波次 2 · G8 项目编排器（orch-fixer，完成）
23 项：21 ✅ + 2 🔧 + 1 ❌（G8-04 exec 保持 fail-closed 不可自动重试，避免绕过审查门禁重审）。重点：协议动作优先收口（G8-14）；提示词文件符号链接拘禁 + 64KB 上限 + 容错（G8-19/20）；双锁合并与原子应用（G8-09/11 🔧）；project_path 命令边界 fail-closed 校验（G8-12）。新增 15 测试。

### 波次 2 · G9 聊天与运行循环（chatloop-fixer，完成）
15 项：13 ✅ + 1 🔧（UsagePauseGuard RAII）+ G7-11 调查后移波次 3（需前后端协同）。G9-07/08/09/10（事件协议）调查结论：当前无运行时受损面，属前瞻加固，随波次 3 统一实施。重点：tool run 收尾一体化消除悬挂记录；会话级工作区隔离；append 与 DB 加载同口径过滤；提示词文件原子写；每轮重建系统提示。新增 9 测试。

### 波次 2 · G10 LLM/摘要/调试日志（llmlog-fixer，完成）
15 项：13 ✅ + 2 🔧。重点：local_image_path 拘禁聊天图片目录；enable_thinking Option 化 + 400 重试剥离；debug.rs 完整重构为后台写入线程+mpsc（无锁 send、无 runtime 依赖、10MB 轮转）。新增 18 测试。

### 波次 2 · G11 状态与命令（state-fixer，完成）
15 项（含 G7-06 移交）：7 ✅ + 8 🔧。重点：DispatcherState::new 全异步化（阻塞 I/O 入线程池，失败弹对话框不裸 panic）；ActiveRunHandle RAII + epoch 代际守卫；GenerationGate 原子化；resume 有界通道去重 + 复查 cancel；project_path 双重包含校验（按实际信任模型调整了报告原建议）。新增 15 测试。

### 波次 2 · G12 子智能体管理（submgmt-fixer，完成）
15 项：14 ✅ + 1 ❌（G12-13 确认机制需命令签名+前端配套，列跨界）。重点：缓存/DB 键一致性强制校验；解析失败 fail-closed 不覆盖缓存；async 命令全部 spawn_blocking；坏 JSON 分类隔离。新增 7 测试。

### 波次 2 · G13 子智能体运行时（subrt-fixer，完成）
11 项（含 G1-20 移交 + tool.rs spawn_blocking 附加项）：8 ✅ + 3 🔧。重点：工具超时无损收口（剩余预算 timeout + fail-closed 上报）；重试资格状态机（按工具名记录失败轮数）；force_final_response 严格执行；按轮次滑动窗口裁剪（≤120k 字符）；trace 缓冲 500 条上限 + 审计标记。新增 8 测试。运行中曾因 API 错误中断一次，经恢复续作完成。

### 波次 2 集成验证 + 协调清理（2026-08-11）
`cargo check` 0 错误；`cargo test --lib` **312 passed / 0 failed**。
协调清理：ToolProvider trait 收窄（移除未用 provider_name/specs_for_workspace）、删除无调用方的 with_artifacts、ToolInput 移除构造即弃的 call_id/arguments 字段。
残余：db/messages.rs 的 add_visible_message* 死代码待 kwrename 批次结束后删除（避免同文件并发冲突）。

### 波次 3 · 图事件协议（graphproto-fixer，完成）
G4-12 ✅：GraphRunEvent 新增 NodeCancelled 变体（camelCase 协议锁定测试），mark_node_cancelled 改发新变体，前端 graph-store reducer 分支 + vitest 用例；RunCancelled 不扩节点清单（hydrate 回源，决策成文）。
G5-26 ✅：runner/node_task/node_exec/scheduler 状态字面量全部接入集中常量；enum 化经评估明确不做（落库值兼容/字符串读取世界/phase 开放集三条理由写入 types.rs）；测试字面量按「锁定落库值」约定保留。
验证：313 测试绿、pnpm build + vitest 41 用例通过、DB 落库字符串值零变化。

### 波次 3 · keywords 列改名（kwrename-fixer，完成）
G7-03 ✅：SCHEMA_VERSION 27→28（RENAME COLUMN 迁移 + 悬空清理 + 索引重建 + FK 校验，幂等守卫）；keywords/sessions/messages/commands 全链路改名 sessionId；前端 types/SessionPanel/use-chat-queries 同步；**清单外发现** scripts/backfill_chat_keywords.py 消费方一并修复并加旧库守卫。313 测试 + pnpm build 通过。
插曲：期间协调者死代码清理误删 with_artifacts 遗留测试致编译阻塞，kwrename 应急处置代补解锁，最终由协调者统一收口（方法+测试双删），全树一致。

### 波次 3 · 运行循环事件协议（runloop-proto-fixer，完成）
G9-07 ✅ tool_call_id 必填化 + ToolFinished 补 arguments；G9-08 ✅ AssistantDelta/ThinkingDelta 加 seq + last_seq 贯穿；G9-09 ✅ 预留事件文档化（核实前端已有 handler，保留）；G9-10 ✅ AgentTurn 补 Clone；G7-11 ✅ Finished 轻量化（计数负载 + 前端改拉取，AgentTurn.messages 与孤立查询删除）。
收尾：G9-14 残余（序列化失败改 Result 上抛，理由：协议正确性优先于回喂）、G9-05 残余（db 侧过滤 delegate 到 common 单一实现）、add_visible_message* 死代码删除（基线警告清零）。
验证：cargo check --all-targets 零错误、313 测试绿、pnpm build + lint（--max-warnings 0）通过。
风险：Finished/AgentTurn 为前后端契约变更，必须同版本发布。

### 最终集成验证（2026-08-11，全部波次完成后）
- `cargo check --all-targets`：**0 错误**（1 条已知警告：ToolSummary* 预留事件变体未构造，G9-09 有意保留并文档化）
- `cargo test --lib`：**313 passed / 0 failed**
- `pnpm build`（tsc + Vite）：通过；`pnpm lint`（--max-warnings 0）：通过
- vitest（graph-store reducer 等）：41 用例通过
- 全程新增测试约 120 个（纯函数/迁移/并发/序列化锁定）；SCHEMA_VERSION 26→28（v27 悬空清理 + v28 列改名，均幂等带测试）

### 处置总账
245 条问题 = **239 ✅/🔧（修复或完整重构）+ 6 ❌（调研后否决，均附技术理由）**。
否决清单：G1-11（签名变更破坏 5 处调用点且行为回归）、G1-22（现注入链路无实际漂移，不变量已固化）、G6-16（日志本就存在，报告针对旧形态）、G7-09（累加即计费口径，补注释）、G8-04（自动重试绕过审查门禁，fail-closed 保留）、G12-13（需命令签名+前端确认 UI 配套，超出边界）。
范围外遗留（已记录，供后续决策）：sidecars/pi-agent failed 消息上报 usage（TS 侧）；工具级协作式取消令牌；exec 重试与审批状态关联；sub_agent_force 前端确认 UI；llm.rs/summary.rs/local_zsh.rs 超 500 行拆分；cargo fmt 存量治理。
